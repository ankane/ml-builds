name: XGBoost
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          sudo apt install cmake
          git clone --recursive https://github.com/dmlc/xgboost
          cd xgboost
          mkdir build
          cd build
          cmake ..
          make -j4
          du -ah .
